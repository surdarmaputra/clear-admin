---
interface Props {
  class?: string;
}

const { class: className = 'h-4 w-full' } = Astro.props;
---

<div
  aria-hidden="true"
  class:list={['animate-pulse rounded-control bg-surface-hover', className]}
></div>
